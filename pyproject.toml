[project]
name = "meshtech-phone"
version = "00.000.002"
description = "Client apps for the MeshTech Scope system (web / android TWA / apple / tools branches; shared base on main)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["app/src/lib"]

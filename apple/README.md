# meshtech-phone apple/ - the iOS variant

EMPTY ON PURPOSE (seeded 2026-09-22).

The honest limits (from PHONE-APP-DESIGN.md): Apple blocks browser
BLE, so iPhone gets the WiFi/TCP path only (stage 1) and never the
BLE radio mode (stage 2). This folder holds the same web/ source
with an apple-touch-icon and any iOS-specific tweaks.

Nothing lands here until after stage 1 is done.
